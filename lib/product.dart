class Product {
  String id, name, price, quantity, size, type, date;
  Product({
    this.id,
    this.name,
    this.price,
    this.quantity,
    this.size,
    this.type,
    this.date,
  });
}
